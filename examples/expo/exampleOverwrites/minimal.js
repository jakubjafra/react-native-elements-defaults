import { StyleSheet } from 'react-native';
import { TextOverwrite } from "react-native-elements-defaults";

export default [
    new TextOverwrite(StyleSheet.create({
        style: {
            fontFamily: 'Times New Roman',
        },
    }))
];
